services:
  - type: web
    name: rag-chatbot
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 10000 --app-dir backend"
    envVars:
      - key: PYTHON_VERSION
        value: "3.10.13"
